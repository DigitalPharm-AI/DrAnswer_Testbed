# Bedrock credential scope for the Agent API and async worker only.
# Install the real value with install_bedrock_token.sh. Never put it in
# agent.env, a release archive, a command-line argument, or shell history.
AWS_BEARER_TOKEN_BEDROCK=
