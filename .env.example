# This all-in-one env file is still loaded as a fallback.
# Prefer .env.agent_app, .env.system_app, and .env.phr_app for app-specific settings.
APP_ENV=development
LLM_PROVIDER=bedrock_anthropic
LLM_MODEL_TIER=fast
LLM_FAST_MODEL=global.anthropic.claude-haiku-4-5-20251001-v1:0
LLM_SONNET_MODEL=global.anthropic.claude-sonnet-5
LLM_MAX_TOKENS=4096
LLM_TEMPERATURE=0.2
AGENT_TRACE_LOGGING=true
SYSTEM_TRACE_LOGGING=true
AWS_REGION=ap-northeast-2
AWS_BEARER_TOKEN_BEDROCK=your-bedrock-api-key
# AWS_ACCESS_KEY_ID=your-aws-access-key-id
# AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
# AWS_SESSION_TOKEN=your-aws-session-token
# AWS_PROFILE=your-aws-profile
AGENT_BASE_URL=http://127.0.0.1:8001
SYSTEM_BASE_URL=http://127.0.0.1:8000
PHR_BASE_URL=http://127.0.0.1:8002
# Set the same non-empty value in agent/system env files to protect internal mutation APIs.
# Required when APP_ENV=production.
INTERNAL_API_TOKEN=
SYSTEM_DATABASE_URL=sqlite:///./data/system.db
AGENT_DATABASE_URL=sqlite:///./data/agent.db
# Recommended for agent async queue on a single EC2 instance:
# AGENT_DATABASE_URL=postgresql+psycopg://da_drug:change-me@127.0.0.1:5432/da_drug_agent
AGENT_TASK_MAX_ATTEMPTS=3
AGENT_TASK_VISIBILITY_TIMEOUT_SECONDS=300
AGENT_TASK_RETRY_BASE_SECONDS=30
AGENT_TASK_RETRY_MAX_SECONDS=600
# Keep false in production and run `python -m agent_app.worker_main` as a separate process.
AGENT_EMBEDDED_WORKER_ENABLED=false
AGENT_WORKER_HEARTBEAT_INTERVAL_SECONDS=10
AGENT_WORKER_STALE_AFTER_SECONDS=60
PHR_DATABASE_URL=sqlite:///./data/phr.db
PROMPT_WORKBOOK_PATH=data/prompt_registry.xlsx
POLICY_WORKBOOK_PATH=data/default_notification_policies.xlsx
PRO_CTCAE_WORKBOOK_PATH=data/pro_ctcae_korean_parsed.xlsx
PRO_CTCAE_EMBEDDING_PROVIDER=auto
PRO_CTCAE_EMBEDDING_MODEL=amazon.titan-embed-text-v2:0
# auto는 Claude LLM 호출에 쓰는 Bedrock 인증을 우선 재사용해 Bedrock embedding 모델을 호출합니다.
# Voyage를 쓰려면 provider/model/key를 아래처럼 바꾸세요.
# PRO_CTCAE_EMBEDDING_PROVIDER=voyage
# PRO_CTCAE_EMBEDDING_MODEL=voyage-3.5
# VOYAGE_API_KEY=your-voyage-api-key
# These values seed POLICY_WORKBOOK_PATH only when the workbook is first created.
MISSED_DOSE_GRACE_MINUTES=90
POLICY_DEFAULT_EXTRA_REMINDERS=1
POLICY_DEFAULT_INTERVAL_MINUTES=30
